import { FC, memo } from "react";
interface Props {
}
const AssignmentPage: FC<Props> = (props) => {
    return (
        <div>
            this is assignment oage
        </div>
    );
};
AssignmentPage.defaultProps = {
}
export default memo(AssignmentPage);